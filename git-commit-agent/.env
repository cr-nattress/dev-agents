# Git Commit Agent Environment Configuration

# These values will override the central .env settings if uncommented

# GitHub Token (for authentication with GitHub)
# GITHUB_TOKEN=your_github_token

# Git Author Information
# GIT_AUTHOR_NAME=GANON
# GIT_AUTHOR_EMAIL=your.email@example.com

# Agent-specific settings
COMMIT_TIMEOUT=300
DEFAULT_BRANCH=main

# Default commit message template
DEFAULT_COMMIT_MESSAGE="[%BRANCH%] %MESSAGE%"
